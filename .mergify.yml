queue_rules:
    - name: default
    conditions:
      - "#approved-reviews-by >= 1"

 pull_request_rules:
   - name:
   conditions:
      - "#approved-reviews-by >= 1"
  actions:
     - queue:
        name: default
